node_modules/
.next/
out/
dist/

.env*
!.env.example

# Supabase CLI local link cache (project ref/pooler URL metadata, not secrets, but local-only)
supabase/.temp/

*.log
tsconfig.tsbuildinfo
.DS_Store

# Bulk OSM extract consumed by scripts/import-osm.mjs — 28MB, regenerable from the
# Geofabrik nepal-latest.osm.pbf extract, so it doesn't belong in git.
db/imports/osm-nepal.ndjson
